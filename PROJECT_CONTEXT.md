# PROJECT_CONTEXT

## 1. 프로젝트 한 줄 정의

`Escape the Data Room`은 Python 기초 문법을 한 번 배운 학생들이 방탈출형 문제 상황 안에서 Python을 다시 꺼내 쓰도록 유도하는 웹 기반 복습 활동 도구이다.

## 2. 프로젝트 배경

Python 기초 수업에서는 변수, 리스트, 조건문, 반복문, 문자열 처리, 딕셔너리, 함수 같은 문법을 배운다. 하지만 초보 학습자는 문법 자체를 따라 쓸 수 있어도 실제 문제 상황에서 어떤 문법을 왜 써야 하는지 판단하는 데 어려움을 겪는다.

이 프로젝트는 문법 문제를 추가로 푸는 방식이 아니라, 탈출에 필요한 단서를 해석하는 과정에서 Python이 자연스럽게 필요한 상황을 만든다.

## 3. 실제 사용 상황

이 프로젝트는 정규 학교 수업 전체를 대체하는 플랫폼이 아니다. 디지털새싹, 캠프, 비교과 프로그램, 단기 특강처럼 짧은 Python 수업 이후에 사용하는 복습 활동을 목표로 한다.

사용 상황은 강사나 보조강사가 학생을 지켜보는 오프라인 또는 실시간 수업 환경이다. 학생은 게임을 진행하고, 강사는 학생이 어떤 데이터 처리나 조건 판단에서 막히는지 관찰하며 필요할 때 직접 힌트를 제공한다.

## 4. 대상 학습자

대상 학습자는 Python 기초 문법을 처음부터 배우는 학생이 아니라, 이미 한 번 배운 직후의 초급 학습자이다.

학생은 다음 개념을 들어본 상태라고 가정한다.

- 변수와 출력
- 문자열과 리스트
- 조건문과 반복문
- 간단한 함수
- `split`, `count`, `len`, `set`, `sorted` 같은 기본 데이터 처리

## 5. 해결하려는 문제

해결하려는 문제는 “문법을 아는 것”과 “문제 해결 상황에서 문법을 꺼내 쓰는 것” 사이의 간극이다.

학생은 `for` 문법을 외웠더라도 여러 데이터 행을 반복해서 검사해야 할 때 `for`를 선택해야 한다는 감각이 부족할 수 있다. 이 프로젝트는 탈출이라는 목표를 통해 데이터 필터링, 조건 확인, 중복 제거, 정렬, 후보 검증 같은 상황을 제공한다.

## 6. 핵심 아이디어

방 안에는 여러 단서 오브젝트가 있고, 각 오브젝트에는 데이터가 들어 있다. 학생은 데이터를 직접 눈으로 해석할 수도 있지만, 데이터가 많거나 조건이 복잡해질수록 Python을 사용하는 편이 빠르고 정확하다는 것을 경험한다.

Python은 게임 조작 언어가 아니다. 캐릭터를 움직이거나 게임을 직접 제어하는 수단이 아니라, 단서 데이터를 계산하고 정리하는 도구이다.

비밀번호 구조는 두 층으로 분리한다.

- 퍼즐 비밀번호: 각 단서 오브젝트의 데이터를 분석해서 입력하는 개별 정답이다.
- 룸 비밀번호: Door Keypad에 입력하는 최종 탈출 코드이다.

각 퍼즐을 풀면 서로 다른 퍼즐 비밀번호를 확인하고, 그 보상으로 룸 비밀번호를 추론하는 힌트를 얻는다. 학생은 모든 퍼즐을 반드시 풀지 않아도 되며, 6개 퍼즐 중 4개 정도의 힌트만으로도 논리적으로 룸 비밀번호를 맞힐 수 있게 설계하는 것이 목표이다.

## 7. 교육적 의도

이 프로젝트의 교육적 의도는 다음과 같다.

- Python 문법을 암기 대상이 아니라 문제 해결 도구로 경험하게 한다.
- 학생이 게임 목표를 달성하기 위해 자연스럽게 코드를 실행하게 만든다.
- 단서 분석, 조건 확인, 데이터 필터링을 통해 문법의 사용 맥락을 제공한다.
- 짧은 수업 안에서도 “Python으로 문제를 해결했다”는 경험을 제공한다.
- 강사가 학생의 사고 과정과 막히는 지점을 관찰할 수 있게 한다.

이 프로젝트는 정밀한 자동 평가 시스템이 아니라 활동형 복습 도구이다.

## 8. 발표 방향

발표에서는 기술 구현보다 먼저 왜 이 활동이 필요한지 설명한다.

문제 제기는 “기초 문법을 배운 학생이 실제 문제 상황에서 문법을 선택하고 조합하는 데 어려움을 겪는다”로 잡는다.

해결 아이디어는 “방탈출 게임의 단서 분석 구조와 Python 데이터 처리 활동을 결합한다”로 설명한다.

시연에서는 다음 흐름을 보여준다.

1. 학생이 방에 들어간다.
2. 단서 오브젝트를 클릭한다.
3. 데이터만 봐서는 바로 답을 알기 어렵다.
4. Python Lab에서 간단한 코드를 작성하거나 starter code를 실행한다.
5. 실행 결과로 단서 조건이나 부분 답을 얻는다.
6. 여러 힌트를 종합해 Door Code를 입력한다.
7. 클리어 후 Review Panel에서 푼 문제와 놓친 문제, 작성 코드를 확인한다.

발표의 핵심은 화려한 게임성이 아니라 “Python을 써야 해결이 자연스러운 문제 구조”이다.

## 9. 논문/보고서 작성 방향

보고서에서는 이 프로젝트를 “짧은 Python 수업 이후 활용 가능한 게임형 복습 도구”로 포지셔닝한다.

논리 구조는 다음과 같다.

- 프로그래밍 교육에서는 문법 학습 이후 실제 문제 해결 맥락이 필요하다.
- 초보 학습자는 문법을 배워도 언제 어떤 문법을 선택해야 하는지 어려움을 겪는다.
- 기존의 예제 따라 치기, 단답형 문제, 자동 채점형 코딩 문제는 문법 숙달에는 도움이 되지만 맥락 제공에는 한계가 있다.
- 방탈출 게임 구조는 목표, 단서, 조건 조합을 제공하므로 Python을 도구로 사용하는 상황을 만들 수 있다.
- 이 활동은 학습자가 Python 문법을 문제 해결 맥락에서 다시 사용하도록 유도한다.

효과 검증이 충분하지 않은 상태에서는 “학습 효과가 향상되었다”처럼 단정하지 않는다. 대신 “문법을 문제 해결 맥락에서 사용하도록 유도할 수 있다”처럼 관찰 가능한 범위로 표현한다.

## 10. 개발 방향

개발의 우선순위는 완벽한 게임 엔진이 아니라 발표자가 설명하고 시연할 수 있는 명확한 사용자 흐름이다.

기본 흐름은 다음과 같다.

1. 방 선택 또는 현재 방 진입
2. 방 안의 단서 오브젝트 클릭
3. Inspect Modal에서 상황과 데이터 확인
4. 필요하면 Help Modal에서 문법 레퍼런스 확인
5. Python Lab에서 코드 작성 또는 starter code 실행
6. mock output 또는 실제 실행 결과 확인
7. 답 입력 후 힌트 획득
8. Door Keypad에서 최종 코드 입력
9. Room Clear Review에서 풀이 상태 확인

## 11. 핵심 기능 목록

- 전체 화면형 게임 UI
- Room 0~4 구조
- left / center / right view 전환
- 클릭 가능한 단서 오브젝트
- Inspect Modal
- Python Lab floating window
- 문제별 starter code와 code draft 저장
- mock output 기반 Python 실행 시뮬레이션
- Help / Reference Modal
- 힌트 수집
- Door Keypad
- Room Clear Review Panel
- localStorage 자동 저장
- 새로고침 후 진행 복원

## 12. 제외할 기능 목록

다음 기능은 현재 프로젝트의 핵심이 아니며 V1/V2 범위에서 과하게 만들지 않는다.

- 대규모 LMS
- 교사용 성적 관리 시스템
- 정밀 학습 분석 리포트
- 학생 계정 관리
- 복잡한 강사 대시보드
- 자동 채점 플랫폼
- 3D 게임 엔진
- 캐릭터 이동 중심 게임
- 화려한 최종 아트 파이프라인
- 성적 향상 보장형 기능

## 13. 시연 시나리오

발표 시연은 Room 1 `Signal Room` 중심으로 진행한다.

1. Room 1에 진입한다.
2. `Word Billboard`를 클릭한다.
3. 다수의 단어 데이터와 상황 설명을 보여준다.
4. Python Lab을 열어 starter code를 확인한다.
5. Run 버튼을 눌러 `SIXSEVENONENINE` mock output을 보여준다.
6. 답 `6719`를 입력해 힌트 `CD - AB = 5다.`를 획득한다.
7. 다른 오브젝트를 하나 더 풀어 여러 힌트가 누적됨을 보여준다.
8. Door Keypad에 `7479`를 입력한다.
9. Review Panel에서 푼 문제, 안 푼 문제, 힌트, 코드 draft를 확인한다.

이 흐름은 “모든 문제를 강제하지 않지만, 클리어 후 학습 흔적을 되돌아볼 수 있다”는 설명으로 이어진다.

## 14. 퍼즐 설계 원칙

퍼즐은 숨은그림찾기나 단순 암호 맞히기가 아니라 Python 사용을 유도해야 한다.

좋은 퍼즐은 다음 조건을 가진다.

- 데이터가 충분히 있어 손으로 풀 수도 있지만 Python이 더 자연스럽다.
- 문법을 직접 지시하지 않고 상황과 조건을 제시한다.
- 결과는 Door Code 자체가 아니라 최종 코드를 추론하는 힌트일 수 있다.
- Help는 정답이 아니라 문법 레퍼런스만 제공한다.
- 여러 퍼즐의 힌트를 조합해 Door Code를 추론할 수 있다.
- 퍼즐 정답과 Door Code는 같은 개념이 아니다.
- 퍼즐 정답은 해당 단서 장치를 여는 개별 비밀번호이고, 보상 힌트가 Door Code 추론에 쓰인다.
- 힌트는 중복성과 여유를 갖게 설계한다. 모든 퍼즐을 풀지 않아도, 일부 핵심 힌트만으로 Door Code를 추론할 수 있어야 한다.
- 최종 목표는 방마다 6개의 단서 퍼즐과 1개의 Door Keypad 구조이다. 단, MVP 단계에서는 기존 시연 흐름을 깨지 않는 선에서 우선 5개 단서 퍼즐 + Door Keypad 구조를 안정화하고, 다음 단계에서 6번째 단서 퍼즐을 확장할 수 있다.

사용하기 좋은 문제 유형은 다음과 같다.

- 특정 조건을 만족하는 숫자 필터링
- 문자열에서 패턴 찾기
- 중복 제거
- 이름 또는 ID 매칭
- 시간순 정렬
- 후보 코드 검증
- 논리 조건 계산
- 테스트 로그 분석
- 오류 원인 분류

## 15. UI/UX 방향

UI는 방탈출 게임처럼 보여야 하지만 과하게 화려할 필요는 없다.

중요한 기준은 다음과 같다.

- 16:9 발표 화면에서 잘 보일 것
- 방 화면, 단서, 코드 입력 영역이 명확히 구분될 것
- 단서가 클릭 가능하다는 점이 직관적일 것
- Inspect Modal과 Python Lab이 역할상 분리될 것
- 코드창은 항상 노출하지 않고 필요할 때만 열릴 것
- 텍스트는 발표 화면에서 읽을 수 있을 정도로 정리될 것
- placeholder라도 조잡한 웹 문서처럼 보이지 않을 것

## 16. 구현 우선순위

구현 우선순위는 다음과 같다.

1. Room 1 시연 가능 흐름 유지
2. Room 0~4 전체 구조 완성
3. 각 Room의 퍼즐 데이터 실제화
4. localStorage 저장 안정성
5. Review Panel과 코드 draft 확인성
6. Python 실행기 교체 가능성 유지
7. 발표용 UI 가독성 개선
8. 실제 Pyodide 또는 Skulpt 도입 검토
9. 이미지 에셋 교체 준비

스테이지 수를 늘리는 것보다 하나의 스테이지라도 교육적 의도와 시연 흐름이 분명해야 한다.

## 17. 개발 판단 기준

개발 중 판단이 애매하면 다음 질문으로 결정한다.

- 발표에서 설명하기 쉬운가?
- 학생이 Python을 써야 하는 이유가 분명한가?
- 짧은 수업 상황에서 바로 사용할 수 있는가?
- 강사나 보조강사가 옆에서 관찰하고 도와줄 수 있는가?
- 기능이 과하게 복잡해지지 않는가?
- 보고서에서 교육적 의도를 방어할 수 있는가?
- Room 1 시연 흐름을 깨지 않는가?

## 18. 앞으로 AI 개발자가 지켜야 할 원칙

- 이 프로젝트를 “게임 개발 프로젝트”가 아니라 “Python 복습 활동 도구”로 이해한다.
- 새 기능을 만들 때 발표, 보고서, 실제 수업 상황 중 어디에 기여하는지 확인한다.
- Room, Object, Puzzle, Hint, Code Draft는 데이터 기반 구조를 유지한다.
- Python Lab은 정답 제출기가 아니라 데이터 분석 도구처럼 보이게 한다.
- Help Modal은 정답 힌트가 아니라 문법 레퍼런스로 유지한다.
- 모든 퍼즐을 강제로 풀어야만 진행되는 구조로 만들지 않는다.
- Door Code만 맞히면 Room을 클리어할 수 있어야 한다.
- 최종 이미지가 없어도 placeholder로 진행하되, 나중에 쉽게 교체 가능하게 둔다.
- 실제 Python 실행이 아니면 발표와 문서에서 mock 또는 시뮬레이션임을 과장 없이 설명한다.
- 다음 버전 계획은 구현과 테스트가 끝난 뒤 작성한다.
- 실행 방법이나 테스트 명령이 바뀌면 `run_app.bat`와 `README.md`를 함께 업데이트한다.
