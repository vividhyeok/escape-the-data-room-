# The Code Room — 캡스톤 발표 자료 브리핑

> **이 문서의 용도**
> 이 파일 하나만 LLM(또는 발표 자료 도구)에 입력하면, 프로젝트의 현재 개발 상태와 맥락을 모두 반영한
> 캡스톤 발표 자료(슬라이드 개요, 발표 스크립트, 데모 시나리오, 예상 Q&A)를 만들 수 있도록 작성된
> **단일 컨텍스트 브리핑 문서**입니다. 문서 끝의 "발표 자료 생성 가이드" 섹션에 슬라이드 구성안과
> 활용 프롬프트가 포함되어 있습니다.
>
> 작성 기준일: 2026-06-11 · 작성 시점의 실제 구현 상태를 반영합니다.

---

## 0. 한 줄 요약

**The Code Room**은 SW/AI 캠프나 비교과 파이썬 수업 **초반**에 학생들의 기초 문법 이해 수준을 짧게
**진단**하고, 교사가 학생들이 **실제로 어려워한 개념만 선별해 보완 설명**할 수 있도록 돕는
**방탈출형 게임 기반 형성평가(formative assessment) 도구**이다.

---

## 1. 팀 / 과목 정보

| 항목 | 내용 |
| --- | --- |
| 팀명 | **2ntp** |
| 팀원 | 김민혁, 공원호 |
| 과목 | 2026학년도 1학기 캡스톤 프로젝트(졸업작품) |
| 정식 명칭 | **The Code Room** |
| 개발 코드네임 | ETDR / "Escape the Data Room" (저장소·내부 식별자·배포 URL에 남아 있는 이전 이름) |
| 배포 | Vercel (`https://escapethedataroom.vercel.app`) |
| 저장소 상태 | `main` 브랜치, Supabase 연동 classroom MVP + 방탈출 게임 본편 구현 완료 |

> 참고: 프로젝트 정식 명칭은 **The Code Room**으로 확정되었으나, 코드/배포 URL/일부 문서에는 개발 초기
> 이름인 *Escape the Data Room(ETDR)* 식별자가 그대로 남아 있다. 발표에서는 "The Code Room(개발명 ETDR)"
> 형태로 소개하면 된다.

---

## 2. 문제의식 (Why)

기존 파이썬 기초 수업/캠프 초반에는 다음과 같은 어려움이 있다.

1. **수준 편차가 크다.** 같은 반에도 변수 대입부터 막히는 학생과 이미 반복문을 아는 학생이 섞여 있다.
2. **진단이 느리고 지루하다.** 사전 시험지나 단순 문법 퀴즈는 학생 흥미를 떨어뜨리고, 채점·집계에
   시간이 든다.
3. **교사가 "어디서 막혔는지"를 실시간으로 모른다.** 전체를 처음부터 끝까지 설명하느라 시간을 쓰고,
   정작 학생이 막힌 개념은 짧게만 다룬다.
4. **"전부 다 설명"하는 비효율.** 이미 아는 내용을 반복 설명하면 몰입이 깨지고, 정말 필요한 보완 설명에
   쓸 시간이 부족하다.

→ **핵심 문제:** 수업 초반에 "누가, 어떤 개념에서 막히는가"를 **빠르고 몰입감 있게** 파악할 도구가 없다.

---

## 3. 핵심 아이디어 / 차별점 (What)

- **문법 채점 사이트가 아니다.** 짧은 수업 안에서 파이썬을 "문제 해결 도구"로 다시 꺼내 쓰게 하는
  **활동형 복습/진단** 도구다.
- **방탈출 게임으로 진단한다.** 학생은 "게임을 클리어하려고" 문제를 푸는데, 그 과정의 풀이 로그가 곧
  형성평가 데이터가 된다. → 흥미·몰입 유지 + 진단을 동시에.
- **교사용 운영 콘솔을 제공한다.** 교사는 게임 제작자가 아니라 "수업 진단 활동을 구성하는 사람"으로
  보이는 UI를 사용한다. 문제 세트를 고르고, 6자리 코드를 배포하고, 진행 현황을 모니터링한다.
- **"개입"과 "보완 설명"을 데이터로 돕는다.** 단순 통계 그래프가 아니라,
  - 수업 **중**: "지금 도와주면 좋은 학생"을 즉시 짚어 주고,
  - 수업 **후**: "오늘 실제로 막힌 개념만 짧게 설명"하도록 보완 설명 포인트를 추천한다.

> 한 문장 차별점: **"게임으로 몰입을 유지하면서, 교사에게는 실시간 진단 + 보완 설명 의사결정 데이터를
> 제공하는 형성평가 도구."**

---

## 4. 타겟 사용자 & 사용 시나리오

**대상:** SW/AI 캠프, 비교과 파이썬 기초반, 수업 초반 진단 활동 (예상 소요 15~20분)

**수업 흐름 (7단계) — 이 프로젝트의 목적 그 자체:**

1. 수업 초반에 학생들이 방탈출 게임을 플레이한다.
2. 학생들은 게임을 하면서 파이썬 기초 문법 문제를 푼다.
3. 교사는 학생들의 진행 상황과 풀이 로그를 확인한다.
4. 수업 중간에 못 따라오는 학생을 찾아 보조강사/강사가 개입한다.
5. 수업 후반에 학생들이 가장 어려워한 개념/문제를 확인한다.
6. 교사는 전체 내용을 다 설명하지 않고, 실제로 막힌 개념만 짧게 보완 설명한다.
7. 게임 형식을 통해 학생들의 흥미와 몰입을 유지한다.

---

## 5. 시스템 구성 (큰 그림)

The Code Room은 **하나의 웹 앱** 안에 두 개의 층이 공존한다.

```
┌──────────────────────────────────────────────────────────────┐
│                      The Code Room (웹 앱)                     │
│                                                                │
│  [A] 방탈출 게임 본편 (게임 클라이언트)                          │
│      - Room 0~3, 19개 퍼즐, Python Lab(브라우저 내 실행)        │
│      - 단독으로도 플레이 가능 (#/original)                      │
│                                                                │
│  [B] 교실(Classroom) 운영 레이어  ← 이번 캡스톤의 핵심 확장      │
│      - 교사용 수업 구성 (#/teacher)                            │
│      - 학생 입장 (#/join → #/play)                            │
│      - 수업 진행/결과 대시보드 (#/dashboard)                   │
│      - 풀이 로그를 Supabase에 저장 → 진단 분석                  │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
                   Supabase (Postgres + REST)
        class_sessions · student_sessions · attempt_logs
```

- **[A] 게임 본편**은 기존에 만들어진 자산이며, 이번 작업에서 **구조를 보존**했다(게임 셸·퍼즐·방
  데이터 대공사 금지가 작업 원칙이었음).
- **[B] 교실 레이어**가 게임을 "수업 진단 도구"로 감싸는 부분이며, 발표의 중심이다.

---

## 6. 게임 본편 구조 ([A])

**컨셉:** 데이터로 가득한 방에 갇힌 채, 파이썬으로 데이터를 정돈해 4자리 Door Code를 알아내 탈출한다.

**방 구성 (Room 0~3):**

| 방 | 부제 | 다루는 문법 | Door Code |
| --- | --- | --- | --- |
| Room 0 | 잠긴 서재 (튜토리얼/기초) | 변수 할당·연산·인덱싱·슬라이싱 | 8522 |
| Room 1 | 신호실 | 문자열 메서드(upper·replace·split)·조건문(if/else) | 7479 |
| Room 2 | 기록실 | 반복문(for·while)·리스트·딕셔너리 | 3547 |
| Room 3 | 종합 검토실 (Review) | 놓친 선택 퍼즐 복기 + 작성 코드 회고 | 1584 |

- 각 본편 방의 **필수 퍼즐 4개**를 풀면 Door Code 조각 4개가 완성된다.
- **총 19개 퍼즐**(필수 + 선택). 선택 퍼즐은 난이도가 높고 Room 3에서 복기한다.
- Room은 **3D 파노라마 뷰**(react-three-fiber)로 렌더링되고, 화면 속 오브젝트(TV, 터미널, 캐비닛 등)를
  클릭해 조사한다.

**플레이 루프 (5단계):**

1. **탐색(Explore):** 오브젝트를 조사한다.
2. **데이터 확인(Check Data):** 오브젝트의 Data Surface를 확인한다.
3. **Python Lab 분석:** 코드 에디터에서 파이썬을 작성하고 `Run Analysis` 실행 (브라우저 내 Pyodide 실행).
4. **Unlock Code 입력:** 분석 결과를 해석해 Unlock Code를 입력한다.
5. **Door Code 조각 획득:** 필수 퍼즐이면 조각을 얻어 다음 방으로 진행한다.

**진단과의 연결:** 각 퍼즐은 `mappedPuzzleId`(내부 식별자, 예: `room-0-pattern-tiles`)를 통해 교과서형
파이썬 예제(진단 문제)와 1:1로 매핑되어 있다. 학생이 Python Lab에서 검증(성공/실패)할 때마다
`attempt_logs`에 기록된다.

---

## 7. 교실(Classroom) 레이어 상세 ([B]) — 발표 핵심

### 7-1. 교사용 수업 구성 `#/teacher`

"게임 생성기"가 아니라 **수업 진단 활동 구성 콘솔**로 보이도록 설계.

- **상단:** 제목 "교사용 수업 구성" / 부제 "SW/AI 캠프 초반 진단용 파이썬 방탈출 활동" + 활동 설명.
- **섹션 1 — 수업 정보:** 수업 제목, 대상/상황 칩(SW/AI 캠프 · 비교과 파이썬 기초반 · 수업 초반 진단
  활동), 예상 소요 시간(15~20분), 활동 목적("전체 문법 강의 전, 실제로 막히는 개념을 빠르게 확인").
- **섹션 2 — 문제 세트 빠른 선택:** 기본 진단 세트 / 문자열 기초 세트 / 조건·반복 세트 / 전체 선택 /
  선택 해제. (선택된 문제 id 목록만 바꾸는 프리셋)
- **섹션 3 — 교과서형 파이썬 예제 DB:** 진단 문제 목록(제목·단원·개념·난이도·예상 확인 내용). **내부
  퍼즐 id는 화면에 노출하지 않음.**
- **섹션 4 — 수업 생성:** 선택 문제 수 + 예상 활동 시간 → 수업 생성 → **6자리 클래스 코드 크게 표시**,
  학생 접속 URL, 코드 복사, 학생 입장 화면 열기, 실시간 진행 보기.

### 7-2. 학생 입장 `#/join` → `#/play`

학생이 헷갈리지 않도록 **매우 단순**하게.

- 제목 "ETDR 수업 입장", 6자리 코드 + 닉네임 입력, "수업 코드 확인 후 입장" 버튼.
- 안내 문구: 메인 화면에서 시작 / 풀이 기록은 교사 대시보드에 저장 / **"정답을 몰라도 괜찮습니다. 수업
  초반에 어려운 개념을 찾기 위한 진단 활동입니다."**
- **입력 검증:** 6자리가 아니거나 닉네임이 없거나 존재하지 않는 코드면 입장 차단.
- **진입 시 진행 상태 초기화:** 입장 성공 시 이전 학생의 localStorage 진행 상태로 방 내부에서 시작되는
  것을 막기 위해 게임 진행 상태(`resetProgress`)와 창 레이아웃을 초기화한 뒤 `#/play`로 이동 → **항상
  타이틀/메인 배너부터 시작.**
- **접근 가드:** 세션 없이 `#/play`에 직접 접근하면 게임을 열지 않고 "수업 코드 입력 필요" 안내를
  보여준다. (단, `#/original`은 세션 없이도 게임을 열 수 있어 교사 미리보기에 사용)

### 7-3. 수업 진행/결과 대시보드 `#/dashboard`

"예쁜 통계 화면"이 아니라 **수업 중 개입 + 수업 후 보완 설명 의사결정을 돕는 화면**.

- **실시간성:** WebSocket/Realtime 없이 **수동 새로고침 + 8초 polling(토글 가능)**으로 갱신.
- **요약 카드:** 참여 학생 / 평균 진행률 / 도움 필요 학생 수 / 가장 어려운 개념.
- **전체 진행 요약:** 참여 학생, 아직 시작 안 함, 1개 이상 성공, 전체 성공률, 전체 실패 시도, 선택 문제.
- **지금 도와주면 좋은 학생(개입 추천):** 학생별 사유 문장과 상태 배지. 예) "지민: 문자열 인덱싱 문제에서
  3회 연속 실패", "민수: 입장 후 아직 풀이 시도가 없습니다".
- **학생별 진행 현황 표:** 닉네임 / 진행(CSS 막대 + 해결·선택 문제 수) / 마지막 활동 시간 / 마지막 시도
  문제 / 최근 실패 횟수 / 상태 배지.
- **문제별 성공률·실패·미해결**, **개념별 어려움(하/중/상)**, **수업 후 보완 설명 추천**.
- **차트 라이브러리 미사용 — 모든 막대는 CSS bar.** 교사가 빠르게 읽는 정보 우선.

---

## 8. 진단 데이터 분석 로직 (대시보드의 두뇌)

`attempt_logs`(누가·어떤 퍼즐을·성공/실패·언제)만으로 다음을 계산한다. 정확한 현재 위치 추적 없이
"마지막 시도 문제" 기준으로 충분한 정보를 제공한다.

**학생 상태 라벨 판정 (우선순위 순):**

| 조건 | 상태 |
| --- | --- |
| 시도 기록이 0개 | **아직 시작 안 함** |
| 선택 문제를 모두 성공 | **완료** |
| 마지막 문제에서 2회 이상 연속 실패 | **도움 필요** |
| 마지막 활동이 5분 이상 전 + 진행률 50% 미만 | **도움 필요** |
| 진행률 70% 이상 | **순조로움** |
| 그 외 | **진행 중** |

- **"도움 필요" + "아직 시작 안 함"** 학생이 "지금 도와주면 좋은 학생" 목록에 모인다.
- **가장 어려운 개념** = 개념별 평균 성공률이 가장 낮은 개념.
- **가장 어려운 문제** = 성공률이 낮고 실패가 많은 문제.
- **보완 설명 추천**은 개념별로 구체적 멘트를 생성한다. 예시:
  - "문자열 인덱싱에서 실패가 많습니다. 인덱스가 0부터 시작한다는 점과 `data[0]`의 의미를 짚어 주세요."
  - "반복문 문제에서 누적 변수 초기화를 놓친 학생이 많습니다. `total = 0`을 먼저 두고 반복문 안에서 더하는
    흐름을 보여 주세요."
  - "조건문 문제의 실패율이 높습니다. 비교 연산자와 if/else 분기 순서를 예시와 함께 확인해 주세요."

> 5분 기준은 고정 상수(`STALE_ACTIVITY_MINUTES`)이며 실제 수업 속도에 맞게 조정 가능하도록 주석으로
> 명시되어 있다.

---

## 9. 기술 스택 & 아키텍처

| 영역 | 기술 |
| --- | --- |
| 프론트엔드 | React 19 + TypeScript + Vite |
| 상태관리 | Zustand (게임 진행 상태, localStorage 영속화) |
| 3D 방 렌더링 | three.js + @react-three/fiber + @react-three/drei (파노라마 + 클릭 핫스팟) |
| 코드 에디터 | CodeMirror (@uiw/react-codemirror, lang-python) |
| 파이썬 실행 | **Pyodide(WebAssembly)** — 브라우저 내 Web Worker에서 학생 코드 채점, 서버 불필요 |
| 백엔드/DB | **Supabase** (Postgres + 자동 REST API) |
| 아이콘 | lucide-react |
| 배포 | Vercel (정적 SPA, 해시 라우팅) |

**아키텍처 특징:**
- **별도 서버를 만들지 않았다.** 파이썬 채점은 클라이언트(Pyodide), 데이터 저장은 Supabase로 처리.
- **해시 라우팅 SPA** — 모든 화면이 `#/...` 경로로 전환된다.
- **로그인/회원가입 없음.** 학생은 6자리 코드 + 닉네임만으로 참여(진입 마찰 최소화). 교사 인증도 없음
  (데모/MVP 범위).
- **문제은행은 정적 데이터를 어댑터 함수로 감싼 구조** → 추후 Supabase `problem_bank` 테이블로 교체 시
  화면 구조를 거의 바꾸지 않아도 됨 (확장 가능성 확보).

---

## 10. 데이터 모델

### 10-1. Supabase 스키마 (`supabase/schema.sql`)

```sql
class_sessions   (id, class_code UNIQUE, title, selected_problem_ids text[], created_at)
student_sessions (id, class_code → class_sessions, nickname, created_at)
attempt_logs     (id, class_code, student_id → student_sessions, nickname,
                  puzzle_id, success, error_message, code, created_at)
```

- RLS(Row Level Security)는 켜져 있으나 정책은 **데모/발표용으로 모두 허용(true)**. 실제 서비스에서는
  교사 인증·수업 소유권 검증·학생 접근 제한이 필요하다고 스키마 주석에 명시.

### 10-2. 교과서형 파이썬 예제 DB (문제은행)

현재는 `src/data/textbookProblemBank.ts`의 정적 배열(6문제)을 **조회 함수(어댑터)**로 감싸 사용.

```ts
type TextbookProblem = {
  id; title; unit; concept; difficulty("하"|"중"|"상");
  description; mappedPuzzleId;            // 게임 퍼즐과 매핑
  // 향후 problem_bank 테이블 확장 대비 선택 필드:
  curriculum?; gradeBand?; lessonOrder?; estimatedMinutes?;
  prerequisiteConcepts?; diagnosisTarget?;  // 어떤 오개념을 확인하는지
};
```

현재 6개 진단 문제(예시):

| id | 제목 | 단원 | 개념 | 난이도 |
| --- | --- | --- | --- | --- |
| tb-001 | 변수에 값 저장하기 | 파이썬 기초 | 변수 | 하 |
| tb-002 | 문자열 첫 글자 가져오기 | 문자열 | 인덱싱 | 하 |
| tb-003 | 문자열 일부 자르기 | 문자열 | 슬라이싱 | 중 |
| tb-004 | 조건에 따라 PASS/FAIL 판단하기 | 조건문 | if/else | 중 |
| tb-005 | 반복문으로 합계 구하기 | 반복문 | for | 중 |
| tb-006 | 리스트에서 필요한 값만 고르기 | 리스트 | 필터링 | 상 |

어댑터 함수: `getTextbookProblems()`, `getTextbookProblemById()`, `getTextbookProblemByPuzzleId()`,
`getTextbookProblemsByIds()`, `estimateMinutesForProblems()` + 빠른 선택용 `PROBLEM_SET_PRESETS`.

---

## 11. 라우팅 맵 (데모 동선)

| 경로 | 화면 | 비고 |
| --- | --- | --- |
| `#/` | 랜딩 | 프로젝트 소개 + 3개 화면 진입 |
| `#/teacher` | 교사용 수업 구성 | 문제 세트 선택 → 클래스 코드 발급 |
| `#/join` | 학생 입장 | 6자리 코드 + 닉네임 |
| `#/play` | 게임(학생 활동) | **세션 필수** — 없으면 입장 안내로 가드 |
| `#/original` | 게임(단독 플레이) | 세션 없이 열림 (교사 미리보기/원본 게임) |
| `#/dashboard` | 수업 진행/결과 대시보드 | 클래스 코드로 조회, 8초 polling |

---

## 12. 현재 구현 범위 (Done) vs 한계 (Not yet)

**구현 완료:**
- 방탈출 게임 본편(Room 0~3, 19퍼즐, Pyodide 채점) — 보존.
- 교사용 수업 구성 UI(진단 활동 구성 콘셉트), 문제 세트 빠른 선택.
- 학생 입장 흐름(코드 검증 + 진행 상태 초기화 + 세션 가드).
- 수업 진행/결과 대시보드(학생별 진행, 개입 추천, 문제·개념별 어려움, 보완 설명 추천, polling).
- 풀이 로그 Supabase 저장 및 진단 분석 로직.
- 문제은행 DB 확장 가능 구조(어댑터 계층).
- 빌드 통과(`npm run build`), 모바일 폭에서 입장/교사/대시보드 화면 레이아웃 정상.

**의도적으로 하지 않은 것 / 한계 (정직하게 말할 부분):**
- **로그인/회원가입·교사 인증 없음.** 데모/MVP 범위. (코드만 알면 누구나 접근 가능)
- **실시간(WebSocket/Realtime) 아님.** polling 기반 근사 실시간.
- **문제은행이 아직 실제 DB가 아님.** 정적 6문제 + 게임 퍼즐 매핑. (UI는 "교과서형 예제 DB"처럼 보이도록
  설계, 구조는 DB 확장 대비 완료)
- **문제 편집기 미구현.** 교사는 기존 문제 세트를 선택할 수 있을 뿐, 새 문제를 만들 수 없음.
- **RLS 정책이 데모용 전체 허용** — 실서비스 보안 규칙 아님.
- **위치 추적 없음** — "마지막 시도 문제" 기준의 근사 진단.

---

## 13. 향후 확장 (Roadmap에 쓸 거리)

1. **교과서 문제은행 DB화:** Supabase `problem_bank` 테이블 + 단원/교육과정/선수개념 메타데이터.
   어댑터 함수 시그니처를 유지한 채 정적 배열 → DB 조회로 교체.
2. **교사 인증 + 수업 소유권:** 실제 RLS 정책, 반/학기 관리.
3. **실시간 모니터링:** Supabase Realtime 구독으로 polling 대체.
4. **문제 편집기 / 자체 문제 출제.**
5. **오개념(diagnosisTarget) 기반 자동 보완 자료 추천 고도화.**
6. **누적 학습 분석:** 학생별/학기별 성장 추적.

---

## 14. 발표에서 쓰기 좋은 핵심 문장 (그대로 인용 가능)

- "The Code Room은 SW/AI 캠프나 비교과 파이썬 수업 초반에 학생들의 기초 문법 이해 수준을 짧게 진단하고,
  교사가 학생들이 실제로 어려워한 개념만 선별해 보완 설명할 수 있도록 돕는 방탈출형 게임 기반 형성평가
  도구이다."
- "학생은 게임을 클리어하려고 문제를 풀지만, 그 풀이 로그가 곧 교사를 위한 진단 데이터가 된다."
- "교사용 화면은 게임 생성기가 아니라 수업 진단 활동을 구성하는 콘솔처럼 보이게 했고, 대시보드는 예쁜
  통계가 아니라 '지금 누구를 도울지'와 '수업 후 무엇을 설명할지'를 결정하도록 만들었다."
- "전체 문법을 다시 설명하지 않고, 오늘 실제로 막힌 개념만 짧게 보완 설명한다."

---

## 15. 발표 자료 생성 가이드 (이 문서를 LLM에 먹일 때)

### 15-1. 권장 슬라이드 구성 (10~13장)

1. **타이틀** — The Code Room / 2ntp(김민혁·공원호) / 2026-1학기 캡스톤.
2. **한 줄 정의 + 문제의식** — 수업 초반 진단의 어려움(2장 분량 가능).
3. **핵심 아이디어** — "게임으로 진단한다" 차별점.
4. **사용 시나리오** — 수업 흐름 7단계(스토리텔링).
5. **시스템 구성** — 게임 본편 + 교실 레이어 다이어그램.
6. **데모 ①: 교사** — 문제 세트 선택 → 클래스 코드 발급.
7. **데모 ②: 학생** — 코드 입장 → 방탈출 플레이(퍼즐 1개 시연).
8. **데모 ③: 대시보드** — 진행 현황 + 개입 추천 + 보완 설명 추천.
9. **진단 분석 로직** — 상태 판정 표 + 보완 설명 추천 예시.
10. **기술 스택 & 아키텍처** — Pyodide·Supabase·서버리스.
11. **구현 범위 & 한계** — 정직하게.
12. **향후 확장** — 문제은행 DB화 등.
13. **마무리** — 교육적 의의 한 문장 + 팀 크레딧.

### 15-2. 데모 동선 스크립트 (라이브/녹화 공통)

1. `#/teacher` → "기본 진단 세트" 클릭 → "수업 생성" → **6자리 코드** 확보.
2. 새 탭 `#/join` → 코드 + 닉네임 입력 → 입장 → **타이틀에서 시작** → 퍼즐 1개를 일부러 한 번 틀리고
   다시 맞힌다(실패→성공 로그 생성).
3. `#/dashboard` → 코드 입력/자동조회 → **학생별 진행 + 도움 필요 학생 + 보완 설명 추천** 확인.
   - 시연 포인트: "방금 틀린 학생이 '도움 필요'로 뜨고, 해당 개념 보완 멘트가 자동 추천된다."

### 15-3. 예상 Q&A (준비해 두면 좋은 답)

- **Q. 채점 서버는 어떻게?** → 서버 없이 브라우저 안 Pyodide(WebAssembly)로 실행·채점.
- **Q. 실시간인가?** → 완전한 실시간은 아니고 8초 polling 기반 근사 실시간. 향후 Supabase Realtime로 확장
  가능.
- **Q. 로그인이 없는데 보안은?** → 데모/MVP 범위. 현재 RLS는 전체 허용이고, 실서비스에는 교사 인증·수업
  소유권·접근 제한이 필요함을 한계로 명시.
- **Q. 게임과 진단이 어떻게 연결되나?** → 각 퍼즐이 교과서형 진단 문제와 매핑되어 있고, Python Lab 검증
  결과(성공/실패)가 그대로 풀이 로그로 저장됨.
- **Q. 문제를 교사가 직접 만들 수 있나?** → 현재는 기존 문제 세트 선택만 가능. 문제은행 DB화 + 편집기는
  향후 과제.
- **Q. 기존 시중 코딩 학습 사이트와 뭐가 다른가?** → 문법 채점이 아니라, 짧은 수업 안에서 "누가 어디서
  막히는지"를 게임 몰입과 함께 진단해 교사의 보완 설명 의사결정을 돕는다는 점.

### 15-4. 이 문서로 자료를 만들 때 LLM에게 줄 프롬프트 예시

```
첨부한 "The Code Room 캡스톤 발표 자료 브리핑"을 바탕으로,
2026-1학기 캡스톤 발표용 슬라이드 12장 분량의 발표 대본과 슬라이드별 핵심 불릿을 만들어줘.
- 팀: 2ntp (김민혁, 공원호)
- 청중: 캡스톤 심사 교수/학생
- 톤: 명확하고 자신감 있되, 구현 한계는 정직하게
- 문서의 "발표에서 쓰기 좋은 핵심 문장"을 적절히 인용
- 데모 동선(교사→학생→대시보드)을 라이브 시연 가정으로 구성
- 마지막에 예상 Q&A 6개와 모범 답변 포함
```

---

*문서 끝. 이 브리핑은 작성 시점(2026-06-11)의 The Code Room 저장소 구현 상태를 기준으로 한다.*
